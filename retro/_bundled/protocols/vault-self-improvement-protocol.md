# Vault CLAUDE.md Self-Improvement Protocol

> Load when: running the CLAUDE.md retrospective step of any review cadence.

This file is a living document. AI must treat it as improvable infrastructure, not sacred text. At each review cadence, AI conducts a retrospective on its own operating instructions and proposes concrete updates.

## Trigger: Every Review Cadence

As the **final step** of every Weekly, Monthly, Quarterly, and Annual review, AI must run the CLAUDE.md Retrospective below. This is non-optional.

## CLAUDE.md Retrospective Process

**Step 1 — Collect evidence.** Review the conversation history and session logs since the last review. Identify:
- Instructions the user had to repeat or correct (→ the rule was missing or unclear)
- Moments where AI acted against user intent despite following CLAUDE.md (→ the rule was wrong)
- Workflows that worked well but aren't codified (→ a new rule is needed)
- Rules that were never triggered or are now obsolete (→ candidates for removal)
- User-stated preferences, conventions, or corrections given during sessions

**Step 2 — Draft proposed changes.** For each finding, draft a specific CLAUDE.md edit:
- **Add:** New rule or instruction with rationale
- **Amend:** Modified wording of existing rule with before/after and rationale
- **Remove:** Rule to delete with rationale for why it's obsolete or counterproductive

**Step 3 — Present to user for approval.** Output a structured report:

```
## CLAUDE.md Retrospective — [Review Type] [Date]

### Proposed Additions
- [Rule text] — *Rationale: [why]*

### Proposed Amendments
- **Section X, current:** "[old text]"
- **Proposed:** "[new text]"
- *Rationale: [why]*

### Proposed Removals
- **Section X:** "[rule text]" — *Rationale: [why]*

### No Change Needed
- [Brief confirmation that remaining rules performed well]
```

**Step 4 — Apply approved changes.** After user confirms which changes to accept, update CLAUDE.md directly (this is the one exception where AI modifies CLAUDE.md without copying to workspace first — the file is its own operating manual).

**Step 5 — Cross-pollination check.** Review the sibling Programming CLAUDE.md (your Programming Projects `CLAUDE.md`) for any recently added general rules or patterns worth porting. Propose portable additions to the user — do not auto-apply.

**Step 5b — Skill/Plugin/Command Enrichment Check.** Review the session's work and ask: "Did this session produce a workflow, technique, or capability that an existing skill, plugin, or command should absorb?" Specifically:
- **Existing skills:** Scan skills whose domain overlaps with the work done (e.g., if vault cleaning was done, check `/clean-projects`, `/clean-areas`, `/clean-reference`; if reviews were done, check `/weekly-review`, `/monthly-review`). If the session discovered new steps, edge cases, patterns, or sub-workflows that the skill doesn't cover — propose specific additions to that SKILL.md.
- **Existing plugins/commands:** If the session used a plugin command and found it lacking — propose the enhancement.
- **New skill candidates:** If the session performed a multi-step workflow that isn't covered by any existing skill and is likely to recur — propose creating a new skill via `/skill-create`.
- **Convention updates:** If the session established or validated a practice that should be captured as a shared, cross-project convention — propose adding or amending the relevant convention doc (wherever your setup keeps those).
- Present all proposals to the user. Do not auto-apply. The user decides which enrichments to execute.
- If executing enrichments, read the target skill fully before editing — skills are complex and edits must be surgical.

**Step 5c — Structural editing heuristics.** When applying proposed changes to CLAUDE.md files, prefer these 3 structural patterns:

- **Sibling-rule consolidation:** 3+ bullets describing the same concept → merge into one parent rule with sub-items.
- **Reference-table extraction:** Dense reference tables >30 lines → extract to `Resources/` or `SHARED/` with an inline one-paragraph pointer + key diagnostic commands.
- **Hard-Won Rules extraction:** Multi-paragraph evidence-based rules accumulating in a DO list → extract into a `### Hard-Won Rules (from incidents)` subsection at the same heading level.

These are heuristics, not hard rules. Apply when they improve scannability without losing content. Evidence: 2026-04-15 CLAUDE.md chain audit converged on all three independently from an earlier-same-day retro (commit 454813e).

**Step 6 — Decision trace migration.** Run `/migrate-lessons` to check for unmigrated lessons in `lessons.md` that should be promoted to the decision traces knowledge graph. This ensures mature patterns get indexed for future recall.

## Scope by Review Cadence

| Review | Retrospective Scope |
|--------|---------------------|
| **Weekly** | Tactical fixes — repeated corrections, missing micro-rules, wording clarifications. Lightweight, fast. |
| **Monthly** | Workflow-level — are review processes efficient? Are DO/DON'T lists accurate? Any new patterns worth codifying? |
| **Quarterly** | Structural — is the section organization still right? Are there contradictions between sections? Should sections be added, merged, or split? |
| **Annual** | Strategic — does the vault philosophy still hold? Are the core principles (output-first, evergreen notes, dual hierarchy) still serving the user's evolving goals? Full audit. |

## Guardrails

- **No bloat.** Every proposed addition must pass: "Will this prevent a concrete, recurring problem?" If speculative, discard.
- **No redundancy.** Before adding a rule, check if an existing rule already covers it. Amend instead of adding.
- **Brevity wins.** New rules should be as short as the shortest existing rules. If it takes a paragraph to explain, it's too complex.
- **Reversible.** Every change is logged in the retrospective report. If a rule proves counterproductive, it can be traced and reverted.
- **Changelog.** Maintain a running changelog at the bottom of the Vault Management CLAUDE.md, recording date, review type, and summary of changes made.
- **Changelog row compaction.** Keep each changelog row scannable: one or two-sentence summary + a link to a detail file in your reports folder when the retro spans >300 chars of prose. The protocol file is loaded during every retro — multi-paragraph rows make the file hard to load in full (token limits) and degrade scannability for humans. Same heuristic as Step 5c "Reference-table extraction." Evidence: this protocol file once hit a 25K-token read limit at 188 lines because a single changelog row had ballooned to ~1.2K chars of prose.

## Lessons Learned Log

Maintain a running log (e.g. a `lessons.md` file in your Claude Code resources folder) to capture friction, insights, and what worked between review cadences.

- Entries are raw experience data, not rules.
- Log as issues arise — don't wait for the next review.
- The retrospective process above uses this log as input for proposing CLAUDE.md changes.

---

## CLAUDE.md Changelog

| Date | Review Type | Changes |
|------|-------------|---------|
| 2026-04-22 | Weekly (retro mid-session — post skill-drift discovery on `/project-create` ↔ `/clean-projects` folder convention) | **A1:** `project-create/SKILL.md` Rules — added sibling skill coordination rule requiring auditor skills (currently `/clean-projects`) to be updated in the same change when output conventions shift (folder structure, filename pattern, frontmatter, required/optional sections, Continuation Prompt format). **A2:** `clean-projects/SKILL.md` Phase 0 Convention Self-Check — added step 5 requiring read of `/project-create` SKILL.md to verify folder/layout/template conventions match what Phase 1a scans for. Defense-in-depth with A1 (same pattern as 2026-04-21 meeting-minutes ↔ action-extraction patch). **B1:** `brief-template-compliance.md` — added `## Working Notes` as expected section, `### AI Context` as optional H3 subsection, and new subsection "AI Context — where project-specific AI rules live" formalizing the Brief-as-CLAUDE.md decision: no per-project `CLAUDE.md` files in `Projects/<Brief Name>/`; project-unique AI rules live under `### AI Context` in Working Notes; area-wide rules stay in area-level `CLAUDE.md` (auto-loaded via chain resolution); vault-wide rules in root `CLAUDE.md`. Evidence: folder-wrapped convention landed in `/project-create` weeks before `/clean-projects` was updated; user spotted the drift mid-session while reviewing how project-create behaves. |
| 2026-04-21 | Weekly (retro mid-session — post `/action-extraction` NA-vs-WF misclassification on a client project) | **A1:** Added §"Next Actions vs Waiting For — ownership split" subsection to `brief-template-compliance.md` convention. Canonical rule: NA contains tasks where I'm **at least one** of the owners (including mixed ownership like `@you / @a-teammate`); WF contains `@others-only` items. **A2/A3:** Patched `meeting-minutes/SKILL.md` (Step 4 Next Actions extraction) and `action-extraction/SKILL.md` (Step 4 propagation) with the ownership-split rule — defense-in-depth at both extractor and propagator layers. **M1:** Amended vault root CLAUDE.md Convention Triggers row for the Brief Template Compliance convention — surfaces the rule at load time, not just on deep-dive. **M2:** `feedback_next_actions_me_only.md` memory refined mid-retro (first pass had "mixed → WF"; user corrected to "mixed → NA if I'm one of the owners"). **Q1:** Retroactively corrected a project brief — a mixed-ownership task moved from WF to NA per revised rule. Evidence: `/action-extraction` propagation of meeting minutes from a client project (37 action items, 9 attendees) — 23 of 25 proposed NA items were @others-assigned before user flagged. Two lessons captured in `lessons.md` (ownership rule + "fix at source brief, not narrative summary" — a target blocker lived in the wrong project brief). |
| 2026-04-20 | Weekly (retro — post `/ce:plan` Reporte Ventas multi-module spec) | **E1:** Created `/spec-writer` composite skill at `~/.claude/skills/spec-writer/SKILL.md` — chains `/last30days → /deep-research → /brainstorming → /ce:plan` with hard ordering rule (`/last30days` FIRST), explicit context-forwarding contract per stage, INBOX path override for all intermediate artifacts, abort conditions per stage. First full-pipeline run evidence baked in. **E2:** `/session-close` Phase 2.7 (v0.0.5 → v0.0.6) splits skill codification into atomic (2.7c-atomic) and composite (2.7c-composite) subsections with separate evaluation signals; adds signal #6 (Composite workflow) to the 2.7a scan. **Rejected A1:** CLAUDE.md Convention Triggers row for spec-writing — superseded by creating the skill directly (skill's own description surfaces to future sessions via the skills tool list). **L1–L4:** 4 lessons appended — spec-writing pipeline ordering (`/last30days` first), verify-before-publishing research "Gaps" claims, priority-naming in architecture recommendations, parallel research agents in `/ce:plan` scale ROI with blast radius. |
| 2026-04-20 | Weekly (retro — post `/macro-venezolana`) | **S1:** `macro-venezolana/prompt.md` gains §2b Subasta Mechanism Distinction — retail subasta rate is a BCV auction-policy choice, NOT a sanctions-transmission output. Prevents recurring framing errors in Upside triggers. **S2:** `_shared/nodes/email-scanner.md` binding detection rewritten to use `list_labels` (account-scoped label fingerprint) as canonical; `gmail_get_profile` no longer exposed in current MCP toolset. **L1/L2:** lessons.md — agent rejection-vs-completion harness quirk; startup-day Q1 inheriting continuation-prompt staleness. |
| 2026-04-19 | Weekly (retro — evening, Phase 11 close) | **E1:** Added session-close Phase 2.8 — INBOX Sweep for Session-Generated Files (v0.0.4 → v0.0.5). Includes pending-in-artifact sub-check that scans session-generated reports for buried `## Consolidated Next Actions` items before archive. **F1:** Added changelog row compaction rule to this protocol's Guardrails section. **Rejected:** E2 (routing wiki-lint outputs away from INBOX) — INBOX is intentional staging for user-review artifacts; `/session-close` Phase 2.8 is the correct chokepoint. Evidence: 2026-04-19 Phase 11 close nearly archived 5 deferred items buried in Phase 11 rollup. |
| 2026-04-19 | Weekly (retro) | **A1 (expanded):** Removed Trello scan entirely from `/startup-day` (was Agent 5a). Cadence moved to `/weekly-review` (already Phase 1 Agent E) where the card-level granularity fits better. Startup-day SKILL.md bumped to v0.0.2: (a) shared scanner table dropped Trello row and corrected Reminders model to `haiku`; (b) full scan count changed from "10 agents" to "9 agents"; (c) haiku/sonnet model lists updated (5 moved from sonnet to haiku); (d) added explicit rule: "Never substitute one agent's output for another's — if a scan fails, report the failure explicitly; do not fill the gap with unrelated data." **Evidence:** 2026-04-19 startup-day run — Trello agent ran 17 min / 245 tool calls, timed out internally, and returned Agent 2's (Project Scan) output verbatim as its own result. The scan-substitution rule is the core learning. **A3:** Added inline Deep Work teardown fallback to `/session-close` Phase 0 Step D — when `deep-work-teardown.sh` is missing or non-executable, execute the documented `mkdir + mv` sequence rather than improvising. Makes the skill portable across machines. Evidence: 2026-04-19 — script was missing; fallback was improvised correctly but next session may not. A2 (Investment OS compliance three-tier proximity flag) was surfaced and deferred by user for future session. |
| 2026-04-18 | Weekly (retro) | A1: Amended vault root CLAUDE.md Convention Triggers row for Plan Sync Protocol — broadened from "Sync with Google Workspace" to "Editing any file with `sync_status: production` or `canonical:` in frontmatter, OR syncing". Makes trigger grep-mechanical rather than conceptual. Evidence: session edited a production-synced doc to fix one wikilink without running `/plan-checkout`; auto-repair caught it post-hoc, but the trigger should fire at edit time. Skill codification: `/propagate-amendment` (propagates parent Master Plan structural amendments to child sub-project briefs) approved for creation via `/skill-create`. |
| 2026-04-18 | Weekly (retro — evening, post-funnel-build) | 2 new conventions + 1 CLAUDE.md trigger row + 1 helper script + 1 bug fix + skills audit update. A1: New convention "Two-Path Skill Ingest" (in your shared conventions folder) — codifies Path A (structured ingest, default) + Path B (`--manual` fallback) pattern that shipped twice in `/phase0-email` and `/phase0-prep` refactors. Evidence: 12 AskUserQuestion prompts → ~2 post-refactor; pattern generalizes beyond Phase 0. A2: New convention "External System Source-of-Truth" (in your shared conventions folder) — canonical source-of-truth for Google Workspace artifacts that carry logic (Forms, Apps Script, schema-heavy Sheets) lives in the vault; external artifact is a deployment target. Evidence: a Phase 0 Funnel Redesign shipped 2 forms + 1 Apps Script project with source files in `plugins/cc-onboarding/references/`. B1: Added 2 rows to vault root CLAUDE.md Convention Triggers table (one for each new convention). E1: Audited and fixed stale `gws -a <email>` flag in `okr-update/SKILL.md` (3 occurrences) and `_shared/nodes/gws-subprocess.md` (1 mention) — migrated to `GOOGLE_WORKSPACE_CLI_CONFIG_DIR` env var pattern (the `-a` flag no longer exists in current gws CLI). E2: Promoted ephemeral `/tmp/build_form.py` to a permanent helper script (`gws_form_create.py`, in your shared scripts folder) — generic Forms creator from JSON spec, implements canonical 2-call sequence (create + batchUpdate). E3: Registered `/phase0-invite` (new this session) + 4 other cc-onboarding phase0-* skills in the Skills & Crons Audit doc as a new "Plugin Skills" section. Flagged gap: `/skills-audit` doesn't yet walk `plugins/*/skills/`. |
| 2026-02-09 | Initial | Added Self-Improvement Protocol and Changelog |
| 2026-02-11 | Cross-pollination | Ported from Programming CLAUDE.md: Context Management, Clarify Before Acting, Plan Format, Surgical Edits; Lessons Learned Log |
| 2026-02-11 | Comprehensive review | Fixed: "Auto-update" → "Propose updates"; removed redundant rules; grouped operational protocols; added save location to context management; added risks/tradeoffs to plan template; aligned title with root |
| 2026-02-11 | Cross-pollination | Added Skill Creation; renumbered Changelog |
| 2026-02-12 | Restructuring | Extracted detailed protocol to reference doc; consolidated shared rules to vault root |
| 2026-02-13 | Weekly | Amended Agent Teams rule in vault root: broadened "Teammates" → "Sub-agents and teammates", added "exact output paths" guidance (sub-agent file displacement incident) |
| 2026-02-13 | Weekly | A1: Added file deletion caveat to vault root Agent Teams rule (sub-agents can't `rm`). A2: Added bulk deletion manifest rule to Vault Management DON'T section (non-git vault, irreversible) |
| 2026-02-17 | Project | A1: Added Programs concept to Vault Mgmt folder structure (Programs in Areas, sub-projects in Projects with `parent_program`). A2: Added batch mapping table requirement to vault root Agent Teams. A3: Added mandatory post-batch verification to vault root Agent Teams. M1: Added Program check-in to Weekly review cadence. |
| 2026-02-20 | Weekly | A1: Broadened sub-agent bash caveat from `rm`-only to all bash file ops (mv, rm, cp). Sub-agents should only use Read/Edit/Write tools. |
| 2026-02-21 | Weekly | A1: Added read-only agent enforcement rule to vault root Agent Teams (explicit "DO NOT use Write, Edit, or Bash" in prompts — `bypassPermissions` doesn't enforce read-only). A2: Added long workflow state persistence rule to vault root Agent Teams (checkpoint files for multi-context-window workflows). Updated `/weekly-review` skill: read-only agent prompts, Phase 2 pacing (one section at a time), auto-apply mechanical updates in Next Actions, intermediate state checkpoint after Phase 1, sub-project detail in Programs, user-requested modification exception. |
| 2026-02-27 | Weekly | A1: Amended Evergreen extraction rule — full workflow: create in Evergreen WIP with `source_note` + `## Source`, add `### Extracted Evergreens` to source, source stays in place. Unsolicited proposals go to INBOX. A2: Added proactive cross-linking rule to Linking section — during any vault operation, actively identify and create cross-links (Zettelkasten practice). Updated `/read-review` skill: Disposition Execution section, phase-aware boundary rules, report routing to INBOX. |
| 2026-02-27 | Weekly (retro) | A1: Added file rename cross-link audit rule to Linking section — two-pass audit (wikilinks then plain text), skip archives. Logged two-pass rename pattern to lessons.md. |
| 2026-02-28 | Retro (both) | A1: Added Email Sending Protocol to vault root CLAUDE.md shared behavioral rules — Draft → Review → Revise → Explicit send. Applied to /meeting-agenda SKILL.md Step 8b (revision loop) and operational rules. Triggered by premature email sending after user corrections. |
| 2026-03-07 | Weekly | A1: Added Google Workspace Access rule to Vault Management CLAUDE.md — `gws` CLI primary, MCP backup, Python subprocess for bash escaping. |
| 2026-03-14 | Weekly | R1: Removed "DO NOT modify existing vault files directly" rule from Vault Protection — vault workflows have matured, plans provide approval, and rule created impossible friction for restructuring (81+ file batch ops). A1: Added Enforcement Scripts section (validate_hierarchy.py, sync_program_indexes.py, normalize_frontmatter.py) with integration points. M1: Amended Project Scaffold wording from folder-based to frontmatter-based promotion. M2: Added `approved-snapshot` to canonical status values (Plan Sync Protocol compatibility). |
| 2026-03-14 | Weekly (retro) | A1: Added Scheduling Verification Protocol to vault root CLAUDE.md — check both Work + Personal Gmail calendars before proposing times in any communication. Triggered by sending 4 blocked time slots to external contact requiring correction email. |
| 2026-03-14 | Weekly (retro) | A1: Added Sub-Area MOC pattern to Dual-Hierarchy Model — documents `category: area` files within `Areas/` subdirectories (e.g. one sub-area per organization or client you track). Index files, not programs. |
| 2026-03-14 | Weekly (retro) | Cross-pollination: Fixed 2 incorrect hook documentation rules in Programming Projects CLAUDE.md. M1: Hook output format — corrected from "always exit 0, JSON controls behavior" to "exit 0 = allow, exit 2 = block via stderr". M2: Hook testing — corrected from "echo pipe through bash" to "Python subprocess to avoid bootstrap paradox". |
| 2026-03-14 | Convention Consolidation (retro) | A1: Added convention-index.md to vault management CLAUDE.md Reference Docs table. A2: Logged parallel agent wave pattern to lessons.md. Major session: created 20 canonical conventions in a shared conventions folder, thinned root CLAUDE.md (-34%), vault mgmt CLAUDE.md (-31%), agent-runtime protocols (-41%), updated 9 decision traces, 5 skills. |
| 2026-03-14 | Weekly (retro) | A1: Added Calendar to Shared Protocol Layer table in vault root CLAUDE.md (was in Convention Triggers but missing from protocol table). Logged hub-and-spoke protocol pattern and context pass-through sub-skill pattern to lessons.md. |
| 2026-03-14 | Weekly (retro) | A1: Added Cross-Agent Pipeline Pattern to `sub-agents.md` — time-based ordering with 30-min buffers for multi-agent cron dependencies. A2: Added Agent Operationalization alternate path to `/agent-design` skill (audit → gap analysis → design decisions → fix → cron configs → continuation prompt). Logged 4 lessons: cron dependency mapping, evening pipeline pattern, operationalization vs design, hybrid skill architecture. |
| 2026-03-14 | Weekly (retro) | A1: Added Skill Operationalization Checklist to `sub-agents.md` — 5-step verification (tool existence, path resolution, fs access, exec allowlist, manual test) before enabling cron jobs. A2: Added 2 observations to `agent-edge-cases` collection (SKILL.md tool verification, per-agent fs.workspaceOnly override). Logged 4 lessons: aspirational tool refs, per-agent fs override, CLI early-return misdiagnosis, Claude model reservation for conversational agents. |
| 2026-03-14 | Weekly (retro) | M1: Added AREA/SUB-AREA to Graph-Ready Conventions relationship fields table — 50+ files had plain-string values because convention didn't list them. M2: Added `depends_on` and `priority` to session-closeout brief template. M3: Narrowed haiku batch ops guidance in MEMORY.md — judgment-heavy edits should use Opus or main session (haiku corrupted a file during this session). |
| 2026-03-14 | Weekly (retro) | M1: Fixed gws event creation syntax in `/weekly-prep` and `/weekly-review` skills — `--params` for URL query params, `--json` for request body (were incorrectly combined under `--params`). Logged 4 lessons: gws flag distinction, Read Review checkpoint resilience, AI Learning Audit pre-fill, accli→AppleScript fallback. |
| 2026-03-14 | Weekly (retro) | M1: Strengthened email send guardrail in WORKSPACE-MCP-RULES.md — ALL send operations across ALL methods (MCP, gws, gog, API) require explicit user approval, absolute rule, no exceptions. M2: Restored CLI tool priority rule (gws primary, gog fallback, MCP backup) — lost during convention consolidation. A1: Added multi-account gws pattern (3 accounts, env var switching). |
| 2026-03-16 | Weekly (retro) | A1: Fixed Calendar Protocol row formatting bug in vault root CLAUDE.md (5 cols → 4). A2: Removed 3 duplicate entries from Convention Triggers table (Security Core, Priority Scoring, Calendar Protocol — already in Protocol Layer table). A3: Removed plan-template.md from both mode Reference Docs (kept in vault root only — shared resource). A4: Codified subagent dispatch fallback in deep-work SKILL.md — Ghostty primary, Agent tool fallback when terminal launch fails. Logged 4 lessons: fan-out/fan-in pattern, plan mode friction, premature state archival, corrected baselines. |
| 2026-03-16 | Weekly (retro) | S1: Added Nabu pre-filter step (1b) to `/inbox-clear` SKILL.md — filename-match against `Resources/` before spawning classification agents to skip obvious duplicates from automated sources. S2: Added Extracted Tasks section with User Status column to `/inbox-clear` clearance manifest template — accommodates inline task annotations (DONE, Blocked, Ignore, Delegate). Logged 2 lessons: pre-filter pattern, task annotation pattern. |
| 2026-03-17 | Weekly (retro) | M1: Renamed "Atomic Notes" → "Evergreen Notes" in session-closeout-protocol.md template. M2: Added Evergreen Notes naming rule to Brief Template Compliance convention. M3: Added table wikilink embedding rule (#6) to Graph-Ready Conventions — no separate Link columns. S1: Fixed `/clean-areas` SKILL.md (Atomic→Evergreen + Link column removal). Noted: 80+ existing briefs still use "Atomic Notes" — batch rename deferred to next `/clean-projects` or `/clean-areas` run. Logged 3 lessons: Evergreen naming, table wikilink convention, legacy note conversion workflow. |
| 2026-03-17 | Weekly (retro) | A1: Added Vault Contacts lookup rule to MCP Operating Protocol (vault root CLAUDE.md) — check `Vault Contacts.md` for Slack channels/user IDs before searching MCP. A2: Added batch activation trigger to Plan Sync Protocol convention — documents the batch conversion pattern (create docs → collect URLs → parallel vault updates). Logged 3 lessons: sonnet for section detection, Google Docs batch export pattern, Vault Contacts as channel registry. |
| 2026-03-17 | Session (retro) | A1: Added Spreadsheet Formula Dependency Check rule to vault root CLAUDE.md — must check formula dependencies (FORMULA render) before writing to any column. Triggered by NIVEL-in-D incident that broke all empleado salaries. Logged 5 lessons: column D trap, expense multiplier, Deep Work Ghostty failure, ESPECIALIZACIÓN pattern, TM-based bands. |
| 2026-03-18 | Session (retro) | M1: Amended Spreadsheet Formula Dependency Check — added inverse check: when writing formulas that reference other cells, verify target cells contain expected data via FORMATTED_VALUE read-back. Triggered by Dashboard simulation formulas referencing wrong columns after batch write shifted layout. Logged 2 lessons: batchUpdate column drift, Prestaciones accumulation formula. |
| 2026-03-19 | Session (retro) | E1: Extracted Spreadsheet Formula Dependency Check from vault root CLAUDE.md into a Spreadsheet Safety convention (in your shared conventions folder). Added: FORMULA render fallback (arithmetic inference across 3+ rows), backup-before-write rule, batch write grouping strategy, post-write verification. Vault root reduced from 4 lines to 1-liner + convention trigger. |
| 2026-03-21 | Session (retro) | A1: Created a Meeting Agenda Convention doc (in your shared conventions folder, 27th convention) — `category: meeting-agenda` mandatory, one living doc per recurring meeting, search-before-create. Hookify rule for CC enforcement (warn). Added trigger to CLAUDE.md + convention index. Logged convention-creation-checklist lesson. No CLAUDE.md rule changes needed — the convention system absorbed this cleanly. |
| 2026-03-21 | Session (retro) | M1: Amended MEMORY.md haiku batch ops — `/read-review` agents require sonnet (review blocks exceed haiku context at >4 files). M2: Amended read-review SKILL.md — sonnet for full reviews, haiku only for frontmatter ops, max 5 files/agent. M3: Added frontmatter format spec to read-review agent prompt (kebab-case dispositions, date not boolean for reviewed). Logged 4 lessons: haiku context compression, disposition standardization, two-round cleanup pattern, colon-in-filename edge case. |
| 2026-03-23 | Session (retro) | A1: Added RAW vs USER_ENTERED type safety rule (Branch 6) to Spreadsheet Safety convention — TEXT() wrapper for cross-mode comparisons, default to USER_ENTERED. Triggered by NIVEL type mismatch (numeric vs text) causing silent formula failures in manager review sheets. A2: Created Strategic Design First convention (28th) — pause for model design before implementing complex business processes. Triggered by 3 iterations of salary table formulas before stepping back to ask "what compensation system?" Added to vault root convention triggers table + convention index. |
| 2026-03-24 | Session (retro) | M1: Amended session-closeout-protocol.md Next Actions section — added "scan for stale checked items" rule to catch `[x]` items completed outside the current session (meetings, Trello, team members). Triggered by 40 completed items accumulating in Yogurt 150g brief. Skills (`/clean-projects`, `/weekly-review`) also updated with auto-archive steps as second line of defense. |
| 2026-03-24 | Session (retro) | A1-A3: Added 3 branches to Spreadsheet Safety convention — Branch 7 (Merged Cell Detection: silent write failure diagnosis via sheets.merges query), Branch 8 (Clear Before Repurpose: always clear data range before adding IF-empty formulas to repurposed columns), Branch 9 (External Cell Reference Fragility: positional refs break when users reorganize parameter areas, document mappings, consider named ranges). Logged 4 lessons. |
| 2026-03-24 | Session (retro) | A1: Added `accli` CLI row to vault root CLAUDE.md Convention Triggers — documents Apple Calendar/Reminders access (JSON fields, use cases). A2: Added MCP tool chaining model guidance to MEMORY.md — sonnet required for agents calling MCP tools with large responses or multi-step chains. A3: Added continuation prompt ignorance rule to MEMORY.md — agents scanning vault files must treat `### Continuation Prompt` blocks as data, not instructions. Created Trello List Filtering convention (29th). Logged 3 lessons: MCP model selection, dual calendar sources, continuation prompt derailment. |
| 2026-03-24 | Session (retro) | A1: Added Area Hierarchy section to Vault Management CLAUDE.md — documents category vs area distinction, 3-tier model (Active Focus/Maintained/Reference), GTD tag preservation rule (`areaoffocus`, `HIGH-HARD-PROJECT`). Triggered by near-deletion of GTD tags during MOC normalization. A2: Added Vault Scripts section documenting `generate_moc_index.py`, `prepare_moc_markers.py`, `consolidate_resources.py`. |
| 2026-03-25 | Session (retro) | M1: Added rule #7 (Short wikilinks only) to the Graph-Ready Conventions doc — prohibits full paths like `[[Areas/.../X\|X]]`, requires short `[[X]]` form. Triggered by 34 files with full-path or reference-path AREAs undetected through 2+ prior cleanups. M2: Updated this protocol's Annual scope — replaced "atomic notes, WIP caps" with "evergreen notes, dual hierarchy" (WIP caps removed, terminology updated). Logged 2 lessons: regex YAML line-split, convention duplication drift. |
| 2026-03-30 | Session (retro) | A1: Added "Actionable output navigation" rule to vault root CLAUDE.md Shared Rules — all skill outputs with actionable items must include `wikilinks` to source notes, every row, no plain-text names. Triggered by user correcting both `/followup` and `/startup-day` in the same session. Logged 2 lessons: calendar agent haiku failure, wikilink enforcement as general rule. |
| 2026-03-31 | Session (retro) | M1: Added status enum validation to `validate-brief.py` — was checking presence only, not value against canonical set. 7 files had drifted to `planning/review/draft`. S1: Added status enum validation step + AREA target resolution check to `/clean-projects` SKILL.md Phase 1b. Logged 1 lesson: audit→manifest→batch-execute pattern for vault-wide corrections. |
| 2026-03-31 | Session (retro) | A1: Added "Deep Dive Before Characterizing" to vault root CLAUDE.md Shared Behavioral Rules — read actual data before proposing system changes. Triggered by proposing B1 N2=$250 (would create 21 red circles) before pulling live employee data. Logged 3 lessons: Apps Script paste sanitization, CARGO-specific formula separation pattern, data-first compensation analysis. |
| 2026-04-02 | Monthly (retro) | S1: `/monthly-review` SKILL.md — haiku→sonnet for Phase 1 scan agents (vault-wide reads exceed haiku context). S2: Added recurring-vs-carryover note to Phase 3.7 (monthly checklists are check-ins, not carryover items). S3: Added portfolio review routing note to Phase 3.2 (route through `/investment-report`). S4: Removed Someday-Maybe 50-cap references from Phase 3.6 (cap eliminated by user). |
| 2026-04-03 | Session (retro) | S1: Added exec-approvals.json step (#11) to `/agent-design` SKILL.md — when creating agents with exec-dependent tools, populate allowlist entries as part of installation. Renumbered subsequent config snippets (12→13, 13→14). Updated installation sequence to include exec-approvals in config prompt. Triggered by 12 agents missing exec allowlist entries, blocking all cron jobs. |
| 2026-04-07 | Session (retro) | M1: Fixed session-closeout-protocol.md brief template — AREA from list syntax to scalar `AREA: "[[]]"`, removed empty SUB-AREA list, added missing `parent: "[[]]"` field. Triggered by validator now requiring parent for projects and catching list-format AREA drift. Logged 2 lessons: hook type-filtering pattern, area taxonomy enforcement. |
| 2026-04-07 | Session (retro) | S1: Fixed `/phase0-email` SKILL.md Step 4 — replaced a stale default email account with the current one. Also cleaned 3 other infra files (GWS-RULES.md, calendar.md, contact-card SKILL.md) of the same stale account. Triggered by an email send failure — the old account no longer existed. Lesson: when a default account changes, sweep every file that references it, not just the primary one. |
| 2026-04-12 | Weekly (retro) | A1: Added inverse drift check to `session-close` SKILL.md Phase 2e-sync — auto-repair `sync_status: production` → `development` when a synced doc is edited without `/plan-checkout` first. A2: Amended vault root CLAUDE.md "Deep Dive Before Characterizing" rule — added integration-check clause ("metadata acknowledgment ≠ content integration" when a brief references an Amendment/supersession). M1: Added Branch 1 to the Plan Sync Protocol convention documenting the session-close auto-repair as a safety net with explicit limits (doesn't replace `/plan-checkout`'s pre-edit fetch, changelog discipline, or collaboration signal). Triggered by a cross-team master-plan integration session: user had to explicitly ask whether an amendment had been pushed to the brief (acknowledged-but-not-integrated pattern), and I edited the synced brief without `/plan-checkout`, exposing the inverse-drift blind spot in Phase 2e-sync. Logged 2 lessons: sync-frontmatter drift, acknowledged-vs-integrated gap. |
| 2026-04-11 | Session (retro) | M1: Amended "Deep Dive Before Characterizing" in vault root CLAUDE.md — added "survey existing tools/skills for overlapping functionality before creating parallel infrastructure." Triggered by Library/Wiki Index overlap missed across 3 sessions of LLM Wiki design. Extracted `source-discovery` shared node (2 modes: topic-search, domain-search) for compile + wiki-create. |
| 2026-04-11 | Session (retro) | A1: Added Branch 10 (Safe Tab Migration) to Spreadsheet Safety — create new tab, write clean, update refs atomically, rename/swap, delete old. Never overwrite positions with formula dependencies. Triggered by Dashboard corruption when agents wrote new layout on top of old named-range cells. A2: Added Branch 11 (Agent Delegation for Sheet Writes) — exact values not descriptions, EXECUTE IMMEDIATELY instruction, must-not-overwrite list, read-back verification. Triggered by agents hallucinating labels and overwriting puntaje weight cells. M1: Broadened plans-to-inbox memory to cover all deliverables (paste-ready code, configs) as new timestamped files — Obsidian sync issues caused 3 recreations. |
| 2026-04-08 | Session (retro) | S1: Added task dedup step (step 8) to `/inbox-clear` Phase 1 agent prompt — grep target brief's Next Actions before proposing tasks. S2: Added task creation step (step 3) to `/inbox-clear` Phase 2 execution — append approved tasks to project briefs. S3: Added file existence verification (step 1c) to Phase 1 batch mode — skip phantom files already disposed between sessions. Triggered by 20/24 proposed tasks being duplicates + 4 files already gone from inbox. |
| 2026-04-08 | Session (retro) | M1: Fixed gws path in `/contact-card` SKILL.md — bare `gws` fails in Python subprocess, replaced with the absolute path (e.g. `~/bin/gws`). A1: Added plugin cache sync feedback memory — skills are invisible until cache copy exists, recurring pattern. Logged 3 lessons: check existing skills first, plugin cache staleness, background research agent pattern. |
| 2026-04-11 | Weekly (retro) | S1: Added `_shared/` governance sub-check to `/session-close` Phase 2.5 — surfaces untracked shared-node edits on any session that modifies `_shared/nodes/`. Triggered by modifying `vault-index-loader.md` this session and realizing `_shared/` is untracked in the parent repo. A1: Created a Shared Node Composition convention (41st) — codifies the inline-shared-node-over-sub-skill-delegation principle with concrete token math (5-6× cost differential) and the orchestration-vs-core-logic test. Registered in convention-index.md. Logged 4 lessons: read real instance before designing against schema, shared-node composition cost math, page-overlap over entity extraction, `_shared/` governance gap. No CLAUDE.md rule changes — existing rule "Deep Dive Before Characterizing" covered the data-structure-verification case; this retro recognized I partially applied it and logged the practical extension as a lesson rather than amending the rule. |
| 2026-04-12 | Weekly (retro) | A1: Amended the Agent Team Conventions doc with a parallelization-vs-single-track decision heuristic (new Evaluation Criterion #7 + new Branches entry). Codifies a fleet-automation lesson: parallelize when bottleneck is I/O, single-track when bottleneck is synthesis with context already loaded. Test: "would this sub-agent start by re-reading files I've already read this session?" If yes, keep in-session. S1: Added Phase 2.7f (Skills Audit Registration Check) to `/session-close` SKILL.md — catches new/modified SKILL.md files that aren't registered in the Skills & Crons Audit doc. Prevents skill-registration drift discovered during a multi-phase project registration pass. S2: Added skills-audit reconciliation as a tracked Next Action in the relevant project brief (one-time catch-up; future drift caught by Phase 2.7f). No CLAUDE.md rule changes — existing rules performed well. Session closed at the end of a major multi-phase milestone build. 5 lessons logged today, 4 meta-learnings pushed to a learning-program's Cross-Pollination Log. Decision trace migration deferred — lessons are first-instance. |
| 2026-04-13 | Weekly (retro) | A1: Created a Wire-Level Verification convention (42nd) — when sending data to external systems where byte-level correctness matters, the helper must verify actual wire output on first send per target; a 200-OK response is not evidence of correctness. A2: Added "Send data to external systems" trigger row to vault root CLAUDE.md Convention Triggers table, pointing to the new convention. A3: Created a canonical send helper script (`send_email.py`, in your shared scripts folder) with auto-verification for corporate domains (freemail allowlist carve-out). Verified against a real broken message — caught all 3 real issues (mojibake Subject + missing MIME-Version + missing Content-Type) on a real Gmail fetch. M1: Amended the Email Sending Protocol convention with a Send Mechanism (mandatory) section — all email sends MUST route through the helper; skills MUST surface the `warnings` field to the user. M2: Rewrote `_shared/nodes/gmail-ops.md` — removed the broken plain-text raw-string recipe (RFC 2047 trap for any future skill that copy-pasted it) and replaced the Sending block with a pointer to the helper. M3: Updated `/phase0-email` Step 4 and `/email-triage` Step 5 to use the helper and surface wire-level warnings. Skill example includes explicit "MANDATORY: surface warnings, never silently swallow." Plugin cache synced. Triggered by a 16-day silent email deliverability bug: `gws gmail +send` produced malformed RFC 5322 messages (raw UTF-8 in Subject, no MIME headers) for several Phase 0 prep emails, likely causing some to land in corporate spam folders before the user manually noticed low reply rates. Logged 1 lesson: "call-site success ≠ wire-level correctness" — general pattern for external-system integrations beyond email. Investigation candidates flagged (not fixed): calendar event creation, Slack MCP sends, Sheets writes — all touch the same class and should be audited when next modified. |
| 2026-04-14 | Weekly (retro) | No CLAUDE.md rule changes needed. Clean pilot session for a client project (several phases shipped end-to-end). Logged 5 first-instance lessons to `lessons.md`: pointer-stub consolidation cycle, sub-agent prompt design for read-only audits, fresh-session chain-resolution test, clean-split contract for complementary skills, "no-data PASS" signal for pre-emptively designed validators. All deferred for graduation after 2nd instance. Pushed one meta-learning to a learning-program's Cross-Pollination Log — "per-domain context files as 4th-tier schema pattern validated empirically via a fresh-session test". Decision trace migration deferred (lessons are first-instance). Existing rules (Clarify Before Acting, Surgical Edits, Deep Dive Before Characterizing, Phase 2.5 scope-to-session-only, Batch Operations, Agent Team Conventions) all performed well. |
| 2026-04-14 | Weekly (retro, PM) | Second retro of the day, triggered after a skill-graduation ship (wiki-lint graduated 9 → 12 checks) + a 4-pass Area MOC cleanup (184 → 88 findings) for a specific business area. **A1:** Amended the Agent Team Conventions doc with an Edit-tool parallel-rewrite caveat — for N>5 same-string cross-file rewrites, use a Python bulk script instead of parallel Edit calls. Evidence: 11 of 13 parallel Edits failed in a canonical-routing pass before the Python pivot. **A2:** Amended the Batch Operations doc with a Python `os.walk` verification sweep rule — when a vault-wide Grep for a known-frequent string returns <50 hits, run a secondary Python sweep before trusting the list. Evidence: the initial Grep missed 9 files (5 historical reports + 4 agent-runtime files) masked by a nested `.gitignore`. **A3:** Amended the Brief Template Compliance doc with a Theme-Name Markers vs. Wikilinks section — OKR theme names in area MOCs must be plain text, not ghost wikilinks. Evidence: an Area MOC had 5 theme-name wikilinks flagged as dead; cleanup replaced them with real Programs or plain text. **S1:** Added a Cleanup Playbook section to `/wiki-lint` SKILL.md — documents the 4-pass post-lint cadence (core decision gates → mechanical wins → canonical routing → top-N bulk) with grep-verification between passes as the canonical playbook for first-cleanup runs on any area. **M1:** Strengthened the Meeting Agenda Convention's Rule 2 with an explicit violation example — 3 date-specific agendas were created today for a recurring client meeting despite the rule existing, so the convention needed a concrete violation callout for future enforcement. **M2:** Strengthened the Archive Protection doc with an explicit exemption for archive-stem disambiguation renames — 4 archive files were renamed with a `— Archive` suffix this session, and the convention didn't explicitly permit it before. **Cross-pollination:** ported A1 to Programming Projects CLAUDE.md Implementation Rules → DO list — the Edit-tool constraint affects code-editing sessions too (import renames, API migrations, type updates). No CLAUDE.md rule changes at mode-level. |
| 2026-04-15 | Weekly (retro, PM) | Second retro of the day, triggered after a payroll-sheet session diagnosed a silent scoring failure on a specific row (`AU=FALSE` short-circuited the merit chain) and ported a role-based formula fix from one column to another. Score 2/7 (significant pivot during diagnosis + new pattern discovered). **A1:** Added Spreadsheet Safety §16 — "Structural Verification on Bulk Formula Rewrites." After bulk formula rewrites (>10 cells), verify by reading actual formula text via `valueRenderOption: FORMULA` and inspecting parsed structure, NOT by regex string matching. Tokens like `$E$46` can appear in BOTH old and new formulas in different structural positions (e.g. a 3-arg `CHOOSE` terminator vs. an inline `IF` branch), generating false positives on regex "STALE" flags. Triggered by a column rewrite verification where 267 cells were flagged STALE by regex even though all were the new pattern; resolved by direct formula reads on 2 sample rows. Cheap structural check pattern provided (count argument occurrences, look for new wrappers). **No other rule changes** — debugging discipline + Spreadsheet Safety + Inbox Routing all held. 3 first-instance lessons logged in `lessons.md` (multiplicative gates as silent killers, silent-failure helpers beat fixing the silent failure, structural verification rule). Decision trace migration deferred — single-instance. Skipped a proposed addition (systematic debugging "empirical pushback" note) — judged too soft to codify on one session's evidence. |
| 2026-04-15 | Weekly (retro, EOD) | Third retro of the day, triggered after a `/claude-md-improver` audit of a multi-file CLAUDE.md chain (global router + vault root + vault-management resources + Programming Projects + two domain-specific CLAUDE.md files). Score 2/7 (milestone completed + new pattern discovered). **A1:** Added Step 5c (Structural editing heuristics) to this protocol — documents 3 reusable CLAUDE.md editing patterns that emerged from the audit: (1) Sibling-rule consolidation (3+ bullets describing the same concept → merge into one parent with sub-items; validated by a fix-lifecycle rule merge in one of the domain-specific files), (2) Reference-table extraction (dense tables >30 lines → extract to a Resources or shared folder with an inline pointer; validated by a troubleshooting-tables move in another domain-specific CLAUDE.md, cut 340 → 301 lines), (3) Hard-Won Rules extraction (multi-paragraph evidence-based DO bullets → extract to a `### Hard-Won Rules (from incidents)` subsection; convergent evidence with an earlier-same-day retro that independently landed the same restructure on Programming Projects CLAUDE.md). No rule amendments or removals — existing guardrails held. **Flagged but not retro-scope:** one domain-specific subtree is not git-tracked by the parent repo — its CLAUDE.md and Resources file edits this session landed silently outside version control. Decision deferred to a future infrastructure session: (a) bring it into the tracked repo, (b) make it its own submodule, or (c) accept untracked with documented rationale. No lessons logged — the 3 patterns belong as protocol guardrails, not lessons. Decision trace migration skipped. |
| 2026-04-17 | Session (retro) | Triggered after a participant's Phase 0 onboarding session where 4 workflow gaps surfaced and `/session-close` failed to fire on "lets close out here" (the user had to explicitly request proper invocation). Score 4/7 (milestone + new pattern + protocol gap + user friction). **A1:** Added Semantic Skill Triggers rule to vault root CLAUDE.md Shared Behavioral Rules — documents trigger-phrase → skill mappings (session-close, retro, startup-day, weekly-review) and mandates skill invocation over free-text summary, with an "ask when ambiguous" escape valve. Evidence cited in the rule itself. **A2:** Created a `markdown_to_gdoc.py` helper (in your shared scripts folder) — encapsulates the HTML-intermediate + `files.copy` workaround for Drive `.md → Google Doc` conversion (direct upload returns 400). CLI + Python function API, follows `send_email.py` patterns. **M1:** Amended the Calendar Protocol §5 Event Creation Rules — added `Routine / fitness / morning ritual` row (personal calendar) and `Mentoring / onboarding / external professional session` row (calendar of the sender account that owns the related email thread). New "Sender Consistency Principle" subsection codifies the rule. **M2:** Strengthened `/session-close` skill frontmatter description with an explicit trigger-phrase list for surface-level skill-selection matching. Version bumped 0.0.2 → 0.0.3. **S1 (major):** Rewrote the Phase 0 prep skill (an onboarding plugin): new Step 0 (participant folder scaffold + stale prior-context template scan); Step 1 expanded with GitHub username + Claude subscription tier intake; Step 5 save paths moved from your inbox folder to a per-participant project folder; Step 6 adds GitHub org/username validation and collaborator invite with push permission; Step 8 auto-uploads a Case Study to Drive with `--share` to the member's email; new Step 9 generates a Phase 0 session agenda doc using a prior session as reference template; new Step 10 drafts a pre-work email with 3-bucket structure. Rules section updated. Plugin cache synced. **S2:** OS-aware pre-work in Step 10 — Windows users auto-get a `gh auth login` block (pre-solves the 2021 HTTPS password ban), Mac/Linux users skip it. Claude Pro tier gets a usage-cap warning, Max skips it. Cross-pollination pushed to a learning-program's Cross-Pollination Log. 2 lessons logged to `lessons.md` (Drive .md→Doc requires HTML intermediate; `/session-close` semantic trigger gap). |
| 2026-04-17 | Session (retro, pass 3) | Triggered by a delta-only `/session-close` surfacing two findings not covered by earlier passes: (1) a plugin subtree is untracked by the parent repo — same silent-escape-from-VCS class flagged in an earlier retro; (2) a `send_email.py` In-Reply-To header gap has been faithfully noted in 5 consecutive retros without anyone fixing it — a gap flagged repeatedly without action is a process failure, not a knowledge failure. Score 2/7 (new pattern + protocol gap). **A5:** Generalized the `_shared/` Governance Sub-Check in `session-close/SKILL.md` → renamed to "Untracked Critical Subtree Governance Sub-Check" with a registry table of known untracked subtrees. Generic routing logic checks each registered subtree against session-touched files. Extensible — new untracked-subtree discoveries just add a row. **A6:** Fixed the 5-retro standing gap in `send_email.py`: added a helper that fetches the RFC Message-Id + threadId + existing References chain via a metadata-only fetch; extended `send_email(..., in_reply_to=<id>)` to set proper `In-Reply-To` + `References` headers AND pass `threadId` in the send body (belt-and-suspenders: works for both UI threading and RFC 5322 inter-provider threading). CLI `--in-reply-to` flag added. Threading failures degrade gracefully (send as new thread + add warning to result) rather than crashing. Updated the Email Sending Protocol convention with a "Thread continuity" section documenting the new usage. Signature verified with `--help` + import test. **Process lesson logged:** a gap flagged in 2+ retros must either (a) create a tracked Next Action with owner + due, or (b) escalate to "fix right now" — "note it again" is not acceptable the third time. |
| 2026-04-16 | Session (retro) | Triggered after v4.1 → v5.0.1 Master Plan push cycle + user mental-model correction of sync-status callout. Score 5-7/7 (milestone + new pattern + protocol gap + multi-session checkpoint + minor user friction). **A1:** Swept obsolete "Snapshot congelado — No editar aqui" template out of 3 skills — `/plan-push` Step 9 (+ frontmatter short description, + body opening, + Step 6 verification note, + vault-canonical variant), `/plan-checkout` Step 5, `/canonical-sync` Step 3 + body + log entry. Replaced with `[!info] Sincronizado con canonical — último push <date>` pattern explicitly stating "Este archivo es la copia local (siempre editable)" + both sync operations (`/plan-push` + `/plan-checkout`). All 3 skills gained explicit anti-pattern warning referencing `feedback_plan_sync_local_branch_model.md`. Rationale: user's mental model is git-like (vault = local branch, never "frozen"); the outdated template had been silently misleading users since at least v3.2. Per 2026-04-17 pass-3 "gap flagged in 2+ retros must be fixed" rule — this was the 3rd flagging in a single conversation. **A2:** Codified the table-flatten pre-processor as MANDATORY Step 1a in `/plan-push` Step 6. The flatten_tables() function (23-line Python) converts markdown tables to pipe-separated plain text with bolded header rows, bypassing the `md_to_gdocs.py` native InsertTableRequest path (which has an unresolved cell-index bug on 11×4 tables). Evidence: pattern succeeded twice consecutively — v5.0 push 2026-04-17 + v5.0.1 push 2026-04-16, zero insertTable requests produced, 805 format requests in 3 chunks. Idempotent on table-free content — safe unconditional default. Forward-compatibility note added for future Apps Script DocumentApp.appendTable() path. **Saved:** `feedback_plan_sync_local_branch_model.md` feedback memory (indexed in MEMORY.md). **Deferred to user:** proposal #3 (Task-as-process vs task-as-architecture distinction rule — awaits wording decision). Logged 1 meta-lesson: "3-flag rule" applies recursively — my own retros were the ones flagging without fixing, not just other sessions'. |
| 2026-04-17 | Weekly (retro, PM pass 4) | Triggered after Workstream 1+2 session on Investment OS restructuring (scoring methodology correction + TSLA judgment review + Core Max 20%→35% + crypto allocation overhaul; 15 parallel sonnet agents; 4 DJ entries; 6 violations resolved). Score 4/7 (milestone + new patterns + skill-level gaps + user correction on score values). **No CLAUDE.md rule changes** — existing rules (Clarify Before Acting, Surgical Edits, Deep Dive Before Characterizing, Agent Team Conventions, Batch Operations) all held throughout. **S1:** `/investment-os-check` SKILL.md — thesis assignment must now read `strategic_thesis` from each Position Thesis frontmatter (not hardcoded or inferred). Evidence: today's compliance check mis-flagged TSLA as contributing to Thesis 4 concentration (14.9% violation) when its Position Thesis classifies it as Thesis 1, dissolving the violation. Added explicit "never hardcode position → thesis" rule and a data-gap semantic for positions without a Position Thesis file. **S2:** `/investment-os-check` SKILL.md — asymmetric threshold parsing for PR-01 (equities 20%, BTC/ETH 30%, other crypto 20%) and PF-02 (Thesis 1 40%, Thesis 2 65%, Thesis 4 10%). Skill must parse multi-row tables in the Trigger Library and branch per asset class or per thesis; a scalar interpretation is now a stale-rule defect. **S3:** `/analyze-stock` SKILL.md — interactive judgment-review mode (new Step 2b) and Score History Review Type column. `--challenge` flag launches an interactive dialogue where the user can question any sub-score; the skill walks through the rubric + evidence + score-delta impact and accepts overrides with mandatory rationale capture. Step 7 prompt expanded to 4 options (Approve / Edit / Challenge scoring / Reject). Step 6 §10 Scorecard Summary and new §11 Score History now carry Review Type (Algorithmic / Judgment Review / Mixed). Tier-boundary proactivity rule (rule 14): within 3 points of a tier boundary, skill MUST offer judgment review even without `--challenge`. Evidence: today's TSLA upgrade (71 → 80 via sub-score overrides on Musk 8→10, Ops 3→5, TAM 4→10) was a legitimate judgment-review pattern — codifying it prevents both silent overrides and missing the review opportunity when sub-agent scoring lands near a tier edge. Edits propagate automatically — `~/.claude/skills/<name>/SKILL.md` are hardlinked to the vault canonical (inode check passed). Decision trace migration deferred — today's 4 lessons (documentation-practice drift, tier-floor sensitivity, judgment review legitimacy, layered risk discipline) are first-instance. |
| 2026-04-17 | Weekly (retro, EOD pass 5) | Triggered after `/inbox-clear` session where (a) a permission hook correctly intercepted an ambiguous-signal bulk-delete attempt on 63KB of content files and (b) 138 extracted tasks went uninserted despite the skill claiming to handle task insertion in Phase 2. Score 6/7 (milestone + pivot + new pattern + protocol gap + complex agents + user friction). **No mode-level CLAUDE.md changes** — findings are `/inbox-clear`-scoped. **S1:** `/inbox-clear` SKILL.md manifest template `How to Review` section — added explicit CAUTION block: Decision column is the ONLY place override intent is binding; "DELETE" (or any disposition name) in the Comments column is informational only; Phase 2 will NOT treat it as an override. Phase 2 treats "blank Decision + DELETE in Comments" as "accept proposed disposition, ignore the note". When in doubt, Phase 2 prefers route-to-target over deletion (routing is reversible, deletion is not); permission hooks intercept bulk-rm on ambiguous signals as a final safety net. Evidence: this session I interpreted 3 user "DELETE" Comments as OVERRIDE intent and attempted bulk `rm` on 63KB across Investment OS + Master Plan v4.1 + md-to-gdocs plan; hook correctly blocked. **S2:** `/inbox-clear` manifest template — added `## Questions for User` section between `Items Requiring User Attention` and `How to Review`. Classification agents populate when a note needs clarification before Phase 2 (ambiguous merge target, routing depends on unmade decision, data-integrity concern). Users answer inline with `**Answer:** <text>`; unanswered questions cause Phase 2 to skip and report BLOCKED. Evidence: this session the user asked "which is the stale version?" inline in a MERGE row's Source Destination cell — I answered in the execution report but the template had no affordance for user→skill questions. **A1:** `/inbox-clear` Phase 2 task-insertion step — budget-guard split. Task count ≤ 30 runs inline; > 30 defers to a follow-up task-insertion manifest (`Inbox/YYYY-MM-DD-task-insertion-manifest.md`) with Continuation Prompt. Phase 2 conceptually splits into 2a (mandatory: file ops, deletions, compile, wikilink appends) and 2b (conditional: task insertion across project briefs). Evidence: 138 tasks × 15+ target briefs this session would have burned ~100K additional tokens and risked corrupting Next Actions sections via fragile section detection. **Also fixed:** version frontmatter bumped 0.0.1 → 0.0.2; broken newline (`version: "0.0.1"description:` collapsed onto one line) repaired. Plugin cache: `~/.claude/skills/inbox-clear/` is hardlinked to the vault canonical (same inode), no separate sync needed. Logged 2 lessons: "hook correctness on ambiguous signals — route beats delete when Decision column is blank" + "task insertion at scale is a token cliff — budget-guard skills that fan out across many target briefs." Decision trace migration deferred — both first-instance. |
| 2026-04-18 | Weekly (retro) | Triggered after a participant's Phase 0 email dispatch where `send_email.py account='<personal>'` failed with 401 invalid_rapt mid-send. Score 3/7 (failure/pivot + new pattern + protocol gap). **No mode-level CLAUDE.md changes** — findings are `/phase0-email`-scoped. **A1:** `/phase0-email` SKILL.md Step 4 — tightened the inline comment on the `send_email()` example. Previous comment `account='<work>', # or '<personal>' for personal Gmail` implied both paths worked; updated to explicitly state that the personal-account path fails on this machine (no working `gws` personal-Gmail binding), and to route personal-Gmail sends via the claude.ai Gmail MCP plugin instead. Evidence: today's send failed on first attempt, required a mid-flow pivot to the work account. **Memory updated:** a reference memory appended with the gotcha + practical rule ("default to the work account unless the user explicitly asks otherwise"). **A2 proposed but declined:** email-preference heuristic for Step 1 (corporate vs. personal email prompting) — user opted to handle on a case-by-case basis. **D1 deferred to monthly retro:** an `ai_fluency_level` field (beginner/practitioner/peer) for the Phase 0 prep skill — an AI-peer participant vs. an AI-beginner participant need different Phase 0 session structures; the research agent already surfaces the signal, but the structural skill change belongs in a monthly scope. Logged 1 lesson: "when a skill example comment describes two paths, verify both actually work on the target machine — comment drift from environment drift is silent until something breaks mid-task." Decision trace migration deferred — first-instance. |
| 2026-04-18 | Weekly (retro) | Triggered after the weekly review session. Score 5/7 (full review + 3 skill amendments + 1 canonical new rule + multiple lessons + agent-team execution). **M1:** Root `CLAUDE.md` § MCP Operating Protocol rewritten — new canonical dual-account table: `gws` CLI → your work account · `mcp__google-workspace__*` → your personal account. "Never cross the streams." The `user_google_email` parameter is a hint, not a binding; verify return-message text (not eid decode). Also updated the work-specific `CLAUDE.md` Rule 2 (email → full Workspace). New reference memory added (index updated, flagged for visibility). Evidence: the 5th session in 4 weeks hitting the MCP-routes-to-wrong-account wall when the work-account auth expired. **S1:** `/clean-areas` SKILL.md Phase 1 — added "Verify Before Propose" mandatory section. Each triage finding must reproduce on re-read or drop. Rationale: this session's triage had ~40% false-positive rate (wrong paths for a specific initiative + 3 non-existent programs + 40 programs flagged missing `wikis:` that already had it). **S2:** `/read-review` SKILL.md Batch Mode — added "Scope Contract" at top: process ALL items OR ask first. Never partial-batch silently. Evidence: 19/80 processed without asking until the user called it out mid-review. **S3:** `/reminders-sync` SKILL.md Step 1 — added "Recurring-meeting detection" check before treating reminders as overdue Next Actions. Evidence: a recurring-meeting reminder was flagged overdue when the meeting was already scheduled. **Agent execution:** 3 concurrent teams (haiku frontmatter + sonnet structural + sonnet wiki-linking) completed Phase 2 clean-areas work in ~6 min wall clock (vs 20+ min sequential); 0 overlap because of distinct file-scope ownership. **Also done this session:** 5 calendar events created (work + personal); 4 lessons appended to lessons.md. Decision trace migration deferred. |
| 2026-04-22 | Weekly (retro — post a spreadsheet preview-pane + column-decouple session) | Score 5/7 (milestone + new pattern + protocol gap + multi-session + complex sheet work). **A1:** The Spreadsheet Safety convention gained a "Cascade Preview Pane" branch — pre/post/shift column pattern for high-stakes sheet commits, inline at row level, with color semantics codified (yellow shift, pale green up, pale red down). Evidence: 3 iterations before converging on the right abstraction this session. **A2:** `GWS-RULES.md` §8 Sheets API Quirks added — `values.batchGet` with a `ranges` array fails through the CLI, fall back to multiple `values get`. **A3:** `lessons.md` — one-shot scripts must declare themselves as such at the top (purpose + date + scope + expected disposition). Evidence: the user asked "what is the script for?" mid-session. **M1:** `/retro` SKILL.md Mode Detection — tie-breaker for technical-but-vault-centric sessions: Programming Projects is reserved for tracked git repos; scripts targeting a Sheet via `gws` = Vault Management. **P2.8:** `/session-close` Phase 2.8 substantially rewritten — default posture flipped from "ask per file" to "route by default, keep-in-inbox only with reason"; new content-type classifier table (7 types → destinations → linking actions); linking is now non-optional (move-without-link = orphan); invokes the Inbox Routing, Linking Conventions, and Compile Convention docs by reference. Evidence: the user asked "don't we have an instruction to move all session files out of the inbox and link them?" — the skill had the mechanics but not the forcing function. Decision trace migration deferred — all proposals first-instance. |
